# Netlify deployment configuration
# No setup required - just drag & drop dist/ folder or connect GitHub repo

[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
